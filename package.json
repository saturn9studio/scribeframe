{
  "name": "@saturn9/scribeframe",
  "version": "0.1.2",
  "description": "A format-agnostic text editor engine with owned rendering and widget lifecycle",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saturn9studio/scribeframe.git"
  },
  "type": "module",
  "sideEffects": [
    "./src/styles.css"
  ],
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./styles.css": "./src/styles.css",
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "src/styles.css",
    "docs/API.md"
  ],
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "vitest run",
    "pack:smoke": "node test/packageSmoke.mjs",
    "verify": "npm run demo:typecheck && npm run test && npm run build && npm run pack:smoke && npm run demo:build && npm run browser:test",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build && npm run test && npm run pack:smoke",
    "browser:install": "playwright install chromium webkit",
    "browser:test": "playwright test -c playwright.config.ts",
    "demo": "vite --config demo/vite.config.ts",
    "demo:typecheck": "tsc -p demo/tsconfig.json --noEmit",
    "demo:build": "vite build --config demo/vite.config.ts",
    "demo:build:pages": "vite build --config demo/vite.config.ts --base=/scribeframe/"
  },
  "author": "Saturn9 LLC",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@saturn9/markoffset": "0.2.1",
    "jsdom": "^25.0.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
